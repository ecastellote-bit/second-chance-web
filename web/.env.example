# OpenAI — diagnóstico y semántica
OPENAI_API_KEY=

# Vercel Blob — obligatorio en preview/producción para casos humanos
# Vercel → Storage → Blob (se inyecta BLOB_READ_WRITE_TOKEN al conectar el proyecto)
BLOB_READ_WRITE_TOKEN=

# Forzar almacén durable también en desarrollo local (opcional)
# VOCATIONUP_REQUIRE_DURABLE_CASES=1
